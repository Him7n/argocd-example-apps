apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../kustomize-guestbook

patches:
  - path: add-sidecar.yaml
    target:
      kind: Deployment
      name: kustomize-guestbook-ui
